/** Fixed top logo: centered on mobile, top-left from `md` up. */
export const ZIMA_FIXED_LOGO_POSITION =
  "fixed left-1/2 top-[max(20px,env(safe-area-inset-top))] -translate-x-1/2 md:left-[max(20px,env(safe-area-inset-left))] md:translate-x-0";

/** Search / main Zima landing: top-left on all breakpoints. */
export const ZIMA_FIXED_LOGO_TOP_LEFT =
  "fixed left-[max(20px,env(safe-area-inset-left))] top-[max(20px,env(safe-area-inset-top))]";
